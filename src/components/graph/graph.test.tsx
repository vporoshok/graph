// import { h } from "preact";
// import { shallow } from "preact-render-spy";
// import { GraphComponent } from './index';
// import { Graph } from '../../models/graph';

describe("Initial Test of the Graph", () => {
    test("Graph render an svg", () => {
        // const graph: Graph = {
        //     vertices: [{ x: 34, y: 45 }],
        // };
        // const context = shallow(<GraphComponent graph={ graph } />);
        // expect(context.find("svg").length).toBe(1);
    });
});
